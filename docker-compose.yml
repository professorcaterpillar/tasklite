version: '3.8'

services:
  task-tracker:
    build: .
    container_name: local-task-tracker
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
